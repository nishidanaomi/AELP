// Entradas
const n1 = 5
const n2 = 7
// Processamento
const media = (n1 + n2) / 2
// Saída
console.log(`Média = ${media.toFixed(2)}`)
// Condição
if (media >= 6) {
    console.log('Aprovado!')
}